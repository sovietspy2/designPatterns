{
  "name": "javascriptdesginpatterns",
  "version": "1.0.0",
  "description": "this is a project to practice js desgin patterns",
  "main": "index.js",
  "scripts": {
    "start": "babel-node src/index.js",
    "startOld": "node index.mjs  --experimental-modules",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start2": "npm run build && node dist/index.js",
    "start3": "npm run build && nodemon dist/index.js",
    "nodemonStart": "nodemon --exec babel-node src/server.js",
    "build": "babel index.js -d dist"
  },
  "author": "SovietSpy2",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.3.1"
  },
  "dependencies": {
    "@babel/plugin-proposal-class-properties": "^7.3.0"
  }
}
